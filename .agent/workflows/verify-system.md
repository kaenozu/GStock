---
description: Verify system stability (Build, Lint, Unit Tests)
---
1. Run `npm run build` to verify Type Safety and Build ability.
// turbo
2. Run `npm run lint` to check for code style issues.
// turbo
3. Run `npx vitest run` to verify core logic.
